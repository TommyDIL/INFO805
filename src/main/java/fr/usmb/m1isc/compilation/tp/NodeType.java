package fr.usmb.m1isc.compilation.tp;

public enum NodeType {
    SEMICOLON,
    LET,
    NONE,
    VALUE,
    VAR,
    OPERATOR,
    COMPARATOR,
    WHILE,
    IF_THEN_ELSE,
    OUTPUT,
    INPUT,
    IF_THEN,
    THEN_ELSE,
    UNARY_MINUS
}